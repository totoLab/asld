# [USAGE]
# asld -l [OPTION] [SOURCE] -o [OUTPUT]
#
# [FLAGS] (optional)
# -dep: adds dependencies specified in #configs section to the linking
#
# [SOURCE]
# list of file sources, accepted file extensions are: [nasm, asm, s].
#
# [OUTPUT] (optional) 
# can be specified (!TODO), otherwise it's the basename of the last source